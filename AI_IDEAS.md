# ZIPIMONEY AI 도입 아이디어

## 1. MemeText LLM 교체 (추천)

**현재:** `MemeTextGenerator.java`에 하드코딩된 30개 템플릿에서 랜덤 선택
**변경:** LLM이 매번 다른 맞춤형 한 줄 코멘트 생성

- 지역명, 면적, 소요 기간, 나이 등 컨텍스트 반영
- 위트/공감 있는 문구 자동 생성
- 구현 난이도: 낮음
- 비용: 입출력 짧아서 호출당 거의 무시할 수준
- 방식: `/api/calculate` 호출 시 서버에서 LLM API 호출 → memeText 필드에 담아 응답

### 프롬프트 예시

```
당신은 부동산 자금 계산 서비스의 재미 요소를 담당합니다.
아래 계산 결과를 보고 한국어로 위트 있는 한 줄 코멘트를 작성하세요.

- 지역: {sigungu}
- 면적: {area}평
- 소요 기간: {years}년 {months}개월
- 도달 가능 여부: {reachable}
- 사용자 나이: {age}세

조건:
- 1~2문장, 50자 이내
- 유머러스하되 불쾌하지 않게
- 이모지 사용 금지
```

---

## 2. AI 재무 코칭 팁

**위치:** ResultView 하단, 재미 카드 아래에 "AI 조언" 섹션 추가

- 계산 입력값 기반으로 시뮬레이션 변형 결과를 LLM이 자연어로 정리
- 예: "저축률을 10% 올리면 3년 단축됩니다", "연봉이 500만원 오르면 2년 빨라집니다"
- 구현: 서버에서 변형 시뮬레이션 2~3개 실행 → 결과를 LLM에 넘겨 자연어 요약

### 구현 방식

```
1. CalculatorService에서 변형 시나리오 계산:
   - 저축률 +10% 시 결과
   - 연봉 +500만원 시 결과
   - LTV +10% 시 결과

2. LLM에 원본 결과 + 변형 결과 전달 → 자연어 조언 생성

3. 프론트에 adviceTip 필드로 응답
```

---

## 3. AI 대안 지역 추천

**트리거:** "영원히 못 삽니다" 결과가 나왔을 때

- 동일 조건으로 도달 가능한 다른 지역/면적 추천
- DB에서 최근 거래가 기준으로 후보 지역 추출 → LLM이 자연어 요약
- 구현 난이도: 중상 (지역 간 데이터 비교 로직 + LLM 요약)

### 예시 응답

```
"현재 조건으로 서울 강남구는 어렵지만,
경기도 성남시 분당구 84㎡는 약 8년이면 가능합니다.
인천 연수구라면 5년 안에 도달할 수 있어요."
```

---

## 4. 시장 트렌드 코멘트

**위치:** ComplexBottomSheet 또는 ResultView 상단

- 해당 지역 최근 거래 데이터를 LLM에 넘겨서 한 줄 분석
- 예: "강남구 84㎡는 최근 6개월간 3% 상승 추세입니다"
- DB에 이미 있는 거래 데이터 활용 → 추가 API 호출 불필요

---

## 기술 선택지

| 방식 | 장점 | 단점 |
|------|------|------|
| **Spring AI** | Spring 생태계 통합, 추상화 잘 됨, 스트리밍 지원 | 의존성 추가, 러닝 커브 |
| **직접 HTTP 호출 (Claude API)** | 단순, 의존성 최소, 즉시 적용 가능 | 보일러플레이트 코드 |
| **OpenAI API** | 생태계 넓음, 한국어 품질 양호 | 비용 관리 필요 |

### 추천 순서

1. **1번(MemeText)** 먼저 → 최소 공수로 최대 체감 효과
2. **4번(트렌드 코멘트)** → 기존 DB 데이터만으로 가능
3. **2번(재무 코칭)** → 변형 시뮬레이션 로직 추가 필요
4. **3번(대안 추천)** → 지역 간 비교 로직이 가장 복잡

### 비용 예상 (Claude API 기준)

- MemeText: 입력 ~200토큰, 출력 ~50토큰 → 호출당 약 $0.001 이하
- 재무 코칭: 입력 ~500토큰, 출력 ~200토큰 → 호출당 약 $0.005
- 일 1000회 계산 가정 시: 월 $30~150 수준
